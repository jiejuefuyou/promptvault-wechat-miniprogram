# 微信公众平台 metadata 与审核说明

> 本文件只描述当前代码真实行为。类目、字数、广告和隐私配置规则可能由微信平台调整，提交前应以公众平台当时显示的要求为准。

## 名称建议

```text
AI 提示词小抄 PromptVault
```

避免在名称和简介写死 Prompt 数量；代码会从 `utils/prompts.js` 动态计算。

## 简介建议

```text
本地 AI 提示词工具：按场景搜索内置 Prompt，填写变量后预览和复制；支持收藏与自定义 Prompt。Prompt 内容和变量输入不发送到开发者服务器。
```

若运营版本配置了微信流量主广告，不要使用“完全离线”“零网络”或“不出现广告”等绝对表述。

## 功能介绍

```text
PromptVault 是一个 AI 提示词查找与填写工具。

• 分类、标签与全文搜索
• 中文说明与中英 Prompt 版本
• {{name}} / typed variable / 默认值
• 填写后本机预览与复制
• 收藏、最近打开记录
• 新建、编辑、删除自己的 Prompt
• 静态文章阅读

收藏、自定义 Prompt、搜索历史和最近记录保存在当前设备的微信小程序 storage。没有 PromptVault 账号，也没有开发者云同步服务器。
```

## 当前技术边界

- 内置 Prompt：`utils/prompts.js`
- 本地 storage v2：
  - `pv.customPrompts.v2`
  - `pv.favoriteIds.v2`
  - `pv.recentPrompts.v2`
  - `pv.searchHistory.v1`
- 旧标题型 storage 会在首次启动迁移；只有 v2 全部写入成功后才删除旧 key。
- 详情页通过稳定 Prompt ID 路由，不再把完整 Prompt 快照写入 `currentPrompt`。
- 无登录、无开发者后端、无评论或用户内容上传。
- 复制只在用户主动点击后调用微信剪贴板 API。
- 仓库默认广告位 ID 为空。

## 广告说明

运营者在 `utils/ads-config.js` 配置微信流量主后：

- 首页 Banner 可能在首页打开时联网加载。
- 激励视频只在用户主动点击“看广告后复制增强版”时展示。
- 原始 Prompt 的查看、填写与复制不要求看广告。
- 广告数据处理遵循微信平台与广告服务当时的隐私规则。

如果不配置广告位 ID，相关组件不会展示。不要在未完成平台广告和隐私配置前填入生产广告位。

## 提交审核备注模板

```text
小程序：AI 提示词小抄 PromptVault

【核心功能】
内置 AI Prompt 的分类、搜索、中文说明、typed variable 填写、本机预览与复制；用户可以收藏并在本机创建自己的 Prompt。

【数据】
- 无 PromptVault 登录或账号系统
- 无开发者服务器和云同步
- Prompt 内容、变量输入、收藏、自定义 Prompt、搜索历史与最近记录不上传给开发者
- 上述个人数据仅保存在当前设备的微信小程序 storage
- 静态文章没有评论或上传功能

【权限与 API】
- 不申请位置、相机或麦克风权限
- 用户点击复制按钮时调用微信剪贴板 API
- 当前提交包的广告配置：请在此处明确填写“未启用”或列出实际启用的 Banner / 激励视频广告位

【测试路径】
1. 进入“速查”，搜索任意 Prompt
2. 打开详情，填写变量并复制
3. 收藏后在“最爱”查看
4. 在“我自己”新建、编辑、删除 Prompt
5. 查看“隐私与广告说明”

无测试账号要求。
```

审核备注必须按实际构建填写广告状态，不能复制“无任何网络请求”后又提交带广告位 ID 的包。

## 隐私保护指引文案模板

```text
开发者不建立账号，不收集或上传用户的 Prompt 内容、变量输入、收藏、自定义 Prompt、搜索历史或最近打开记录。这些数据只保存在当前设备的微信小程序 storage，不跨设备同步。

小程序不申请位置、相机或麦克风权限。用户主动点击复制按钮时调用微信剪贴板 API。

小程序运行在微信客户端中，微信可能根据平台规则处理必要的设备、网络、运行日志与安全信息。若当前版本启用微信流量主，首页 Banner 和用户主动触发的激励视频由微信广告组件联网提供，相关处理遵循微信平台隐私规则。

联系邮箱：jiejuefuyou@gmail.com
```

## 发布前一致性检查

```bash
npm run verify
```

并人工确认：

- [ ] `app.js` 版本与上传版本一致
- [ ] 实际 Prompt 数量无需手工改 metadata
- [ ] 广告位 ID 与审核备注、隐私保护指引一致
- [ ] 未启用不存在的账号、云同步、消息推送或用户上传能力
- [ ] 真机上 storage v1 → v2 迁移成功
- [ ] 剪贴板失败有可见反馈
- [ ] 内置详情分享可打开；自定义 Prompt 不通过分享泄露
- [ ] 隐私页和当前构建一致

## 一句话定位

> 微信里的本地 AI 提示词工具：按场景查找，填变量后预览复制，自己的 Prompt 只存当前设备。
