{
  "name": "promptvault-wechat-miniprogram",
  "version": "1.1.0",
  "private": true,
  "description": "Dependency-free contracts for PromptVault WeChat miniprogram local data and rendering.",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "lint": "node --check app.js && node --check utils/prompt-core.js && node --check utils/local-store.js && node --check pages/index/index.js && node --check pages/detail/detail.js && node --check pages/favorites/favorites.js && node --check pages/custom/custom.js && node scripts/verify-miniprogram.cjs",
    "verify": "npm run lint && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
