<!-- pages/detail/detail.wxml -->
<view class="container" wx:if="{{prompt}}">

  <view class="header">
    <view class="title">{{prompt.title}}</view>
    <view wx:if="{{prompt.desc_zh}}" class="desc-zh">{{prompt.desc_zh}}</view>
    <view class="header-row">
      <view class="tags">
        <text wx:for="{{prompt.tags}}" wx:key="*this" class="tag">{{item}}</text>
      </view>
      <view wx:if="{{hasBilingual}}" class="lang-toggle" bindtap="onLangToggle">
        <text wx:if="{{currentLang === 'zh'}}">中 → EN</text>
        <text wx:else>EN → 中</text>
      </view>
    </view>
  </view>

  <view wx:if="{{variables.length > 0}}" class="vars-section">
    <view class="section-title">填一下这些 ✏️</view>
    <view wx:for="{{variables}}" wx:key="key" class="var-row">
      <view class="var-label">{{item.key}}</view>
      <textarea
        class="var-input"
        placeholder="把 {{item.key}} 写在这里"
        placeholder-class="var-placeholder"
        value="{{item.value}}"
        bindinput="onVarInput"
        data-key="{{item.key}}"
        auto-height
      ></textarea>
    </view>
  </view>

  <view class="preview-section">
    <view class="section-title">最终长这样 👇</view>
    <view class="preview-body">{{filledBody}}</view>
  </view>

  <view class="actions">
    <button class="btn" bindtap="onCopy">📋 一键复制</button>
    <button class="btn secondary" bindtap="onWatchAdEnhance" style="margin-top: 16rpx;">
      🎁 看个广告，给你升级版（专家口吻）
    </button>
    <button
      class="btn secondary"
      bindtap="onToggleFavorite"
      style="margin-top: 16rpx;"
    >
      {{isFavorited ? '★ 已经存起来了' : '☆ 收藏这条'}}
    </button>
  </view>

  <view class="hint muted">
    💡 这条 prompt 完全在你手机里运行，输入啥不上传任何地方。
    "看广告升级版" 是给想支持作者的朋友用的，跳过也完全够用。
  </view>

</view>
